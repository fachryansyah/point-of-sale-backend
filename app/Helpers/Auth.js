const jwt = require('jsonwebtoken')

module.exports = {
    user: async (req) => {

    },
    check: async (req) => {

    },
    attempt: async (req) => {

    },
    register: async (req) => {

    },
};
